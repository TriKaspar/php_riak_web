<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>PHP_RIAK the Riak PHP extension</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <link href="/css/phpriak.css" rel="stylesheet">
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <style type="text/css">
        body {
            padding-top: 60px;
            padding-bottom: 40px;
        }
    </style>
    <script src="/js/jquery-2.0.2.min.js"></script>
    {% block header_add %}{% endblock %}
</head>
<body>
{% spaceless %}
<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container-fluid">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
            <a class="brand" href="#">PHP Riak</a>
            <div class="nav-collapse">
                <ul class="nav">
                    {% for item in navigation %}
                        {% if item.dropdown %}
                            {% if item.active %}
                                <li class="dropdown active">
                            {% else %}
                                <li class="dropdown">
                            {% endif %}
                                <a class="dropdown-toggle" data-toggle="dropdown" href="#">{{ item.caption }}<b class="caret"></b></a>
                                <ul class="dropdown-menu" id="swatch-menu">
                                {% for child in item.children %}
                                    <li><a href="{{ child.href }}">{{ child.caption }}</a></li>
                                {% endfor %}
                                </ul>
                            </li>
                        {% else %}
                            {% if item.active %}
                                <li class="active"><a href="{{ item.href }}">{{ item.caption }}</a></li>
                            {% else %}
                                <li><a href="{{ item.href }}">{{ item.caption }}</a></li>
                            {% endif %}
                        {% endif %}
                    {% endfor %}
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </div>
</div>
<div class="container-fluid">
    {% block content %}{% endblock %}
</div>
{% endspaceless %}
<div class="container-fluid">
    <hr />
    <footer id="footer">
        <p class="pull-right"><a href="#top">Back to top</a></p>
        <div class="links">
            <a href="https://github.com/TriKaspar">GitHub Source</a> -
            <a href="https://github.com/TriKaspar/php_riak/issues">Issues</a>
        </div>
        Created and maintained by <a href="https://github.com/TriKaspar">Kaspar Bach Pedersen</a>.<br />
        Built using <a href="http://twitter.github.io/bootstrap/">Twitter bootstrap</a>, <a href="http://bootswatch.com/cerulean/">Cerulean theme</a> and <a href="https://bitbucket.org/kasparp/phoriz">Phoriz framework</a>.
    </footer>
</div>
<script src="/js/bootstrap.min.js"></script>
<script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
            m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-41583897-1', 'bachpedersen.dk');
    ga('send', 'pageview');
</script>
</body>
</html>