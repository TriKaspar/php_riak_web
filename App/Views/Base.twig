<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>PHP_RIAK the Riak PHP extension</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <link href="/css/phpriak.css" rel="stylesheet">
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <style type="text/css">
        body {
            padding-top: 60px;
            padding-bottom: 40px;
        }
    </style>
    <script src="/js/jquery-2.0.2.min.js"></script>
    {% block header_add %}{% endblock %}
</head>
<body>
{% spaceless %}
<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container-fluid">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
            <a class="brand" href="#">PHP Riak</a>
            <div class="nav-collapse">
                <ul class="nav">
                    {% for item in navigation %}
                        {% if item.dropdown %}
                            <li class="dropdown">
                                <a class="dropdown-toggle" data-toggle="dropdown" href="#">{{ item.caption }}<b class="caret"></b></a>
                                <ul class="dropdown-menu" id="swatch-menu">
                                {% for child in item.children %}
                                    <li><a href="{{ child.href }}">{{ child.caption }}</a></li>
                                {% endfor %}
                                </ul>
                            </li>
                        {% else %}
                            {% if item.active %}
                                <li class="active"><a href="{{ item.href }}">{{ item.caption }}</a></li>
                            {% else %}
                                <li><a href="{{ item.href }}">{{ item.caption }}</a></li>
                            {% endif %}
                        {% endif %}
                    {% endfor %}
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </div>
</div>
<div class="container-fluid">
    {% block content %}{% endblock %}
</div>
<script src="/js/bootstrap.min.js"></script>
<a href="https://github.com/TriKaspar" id="github"><img src="https://s3.amazonaws.com/github/ribbons/forkme_right_darkblue_121621.png" alt="Fork me on GitHub"></a>
{% endspaceless %}
</body>
</html>